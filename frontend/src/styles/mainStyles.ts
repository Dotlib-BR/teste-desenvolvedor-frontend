import styled from 'styled-components';

export const MainStyles = styled.div({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',

  paddingTop: 100,

  h1: {
    fontSize: '2.5rem',
    color: '#1875D0',
    marginBottom: 35,
  },
});
